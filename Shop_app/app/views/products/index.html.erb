

<div class="grid" id="final-product-search">

</div> 


  <div class="allProducts_container" id="stapla">

    <div class="bio">
      <p>The OBJECTS Of My Affection: a curate selected of some of the worlds most beautiful objects.</p>
    </div>

     <% Product.find([270, 271, 272, 273, 274, 275]).each_with_index do |product, index| %>

     <div style="margin-left: 233.5px; margin-top: 129.5px; display: block;" class="indivi_product pile-me <%= @classes[index] %>" data-pile="1" >
       <%= image_tag product.img %>
        <%= product.name %>
        <%= number_to_currency product.price, unit: '£' %>
        <% unless index == 5  %>
          <button type="submit" class="btn btn-info addbasket">BUY</button> 
        <% end %>
        
        <%= render :partial => 'product_info', :locals => {product:product} %>
      </div>
      
    <% end %>

     <% Product.find([276, 277, 278, 279, 280, 281]).each_with_index do |product, index| %>
     <div style="margin-left: 700.5px; margin-top: 129.5px; display: block" class="indivi_product pile-me <%= @classes[index] %>"data-pile="2">
       <%= image_tag product.img %>
        <%= product.name %>
        <%= number_to_currency product.price, unit: '£' %>
        <% unless index == 5  %>
          <button type="submit" class="btn btn-info addbasket">BUY</button> 
        <% end %>
          <%= render :partial => 'product_info', :locals => {product:product} %>
      </div>

    <% end %>

     <% Product.find([282, 283, 284, 285, 286, 287]).each_with_index do |product, index| %>
     <div style="margin-left: 1149.5px; margin-top: 129.5px; display: block;" class="indivi_product pile-me <%= @classes[index] %>"data-pile="3">
       <%= image_tag product.img %>
        <%= product.name %>
        <%= number_to_currency product.price, unit: '£' %>
        <% unless index == 5  %>
          <button type="submit" class="btn btn-info addbasket">BUY</button> 
        <% end %>
        <%= render :partial => 'product_info', :locals => {product:product} %>
      </div>
    <% end %>


    <% Product.find([288, 299, 290, 291, 292, 293]).each_with_index do |product, index| %>
     <div style="margin-left: 233.5px; margin-top: 523.5px; display: block;" class="indivi_product pile-me <%= @classes[index] %>"data-pile="4">
       <%= image_tag product.img %>
        <%= product.name %>
        <%= number_to_currency product.price, unit: '£' %>
        <% unless index == 5  %>
          <button type="submit" class="btn btn-info addbasket">BUY</button> 
        <% end %>
        <%= render :partial => 'product_info', :locals => {product:product} %>
      </div>
    <% end %>

    <% Product.find([294, 295, 296, 297, 298, 299]).each_with_index do |product, index| %>
     <div style="margin-left: 700.5px; margin-top: 523.5px; display: block;" class="indivi_product pile-me <%= @classes[index] %>"data-pile="5">
       <%= image_tag product.img %>
        <%= product.name %>
        <%= number_to_currency product.price, unit: '£' %>
        <% unless index == 5  %>
          <button type="submit" class="btn btn-info addbasket">BUY</button> 
        <% end %>
        <%= render :partial => 'product_info', :locals => {product:product} %>
      </div>
    <% end %>

    <% Product.find([300, 301, 302, 303, 304, 305]).each_with_index do |product, index| %>
     <div style="margin-left: 1149.5px; margin-top: 523.5px; display: block;" class="indivi_product pile-me <%= @classes[index] %>"data-pile="6">
       <%= image_tag product.img %>
        <%= product.name %>
        <%= number_to_currency product.price, unit: '£' %>
        <% unless index == 5  %>
          <button type="submit" class="btn btn-info addbasket">BUY</button> 
        <% end %>
        <%= render :partial => 'product_info', :locals => {product:product} %>
      </div>
    <% end %>
  </div>


